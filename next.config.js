// @ts-check

/**
 * @type {import('next').NextConfig}
 **/
const nextConfig = {
    output: "export",

    basePath: "/DevCommunities.github.io",

    images: {
        unoptimized: true,
    },
};

module.exports = nextConfig;